[workspace]
members = ["lib", "program", "script", "visual"]
resolver = "2"

[workspace.dependencies]
alloy-sol-types = "1.0"
petgraph = "0.8.2"
graphalgs = "0.2"
rand = "0.8" # graphalgs

# rand crate sucks. so much renaming from 0.8 to 0.9
